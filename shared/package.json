{
  "name": "@mindforge/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared core layer — auth, interfaces, constants, validation, and utilities for all Mindforge apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
